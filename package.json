{
  "name": "vip-auto-logbook-v2",
  "version": "2.0.0",
  "description": "Cloud logbook for auto service (React/Postgres version)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:client": "yarn --cwd client install && yarn --cwd client build",
    "postinstall": "yarn --cwd client install && yarn --cwd client build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "sqlite3": "^5.1.7",
    "socket.io": "^4.7.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
